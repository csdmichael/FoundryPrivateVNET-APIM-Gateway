name: deploy

on:
  workflow_dispatch:
    inputs:
      deploy_bot:
        description: Deploy the bot function app
        required: true
        type: boolean
        default: true
      deploy_api:
        description: Deploy the API web app
        required: true
        type: boolean
        default: false
      deploy_ui:
        description: Deploy the UI web app
        required: true
        type: boolean
        default: false
  push:
    branches:
      - main

env:
  TF_IN_AUTOMATION: true

jobs:
  terraform:
    runs-on: ubuntu-latest
    env:
      TF_VAR_deploy_api: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_api || false }}
      TF_VAR_deploy_ui: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_ui || false }}
      TF_VAR_bot_app_password: ${{ secrets.BOT_APP_PASSWORD }}
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false
      - name: Validate bot secret configured
        shell: bash
        run: |
          if [ -z "${TF_VAR_bot_app_password}" ]; then
            echo "BOT_APP_PASSWORD secret is not configured. Teams bot auth will fail without it."
            exit 1
          fi
      - name: Terraform init
        run: terraform init
      - name: Import existing Azure resources
        shell: pwsh
        run: ./scripts/terraform-import-existing.ps1
      - name: Terraform validate
        run: terraform validate
      - name: Terraform plan
        if: github.ref != 'refs/heads/main'
        run: terraform plan -var-file=main.tfvars.json
      - name: Terraform apply
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve -var-file=main.tfvars.json

  deploy-bot-function:
    needs: terraform
    if: >-
      github.ref == 'refs/heads/main' &&
      (github.event_name != 'workflow_dispatch' || inputs.deploy_bot)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy bot web app
        uses: azure/webapps-deploy@v3
        with:
          app-name: func-fdryvnetgw-bot-eastus
          package: bot-function
      - name: Verify bot health endpoint
        shell: bash
        run: |
          curl --fail --silent --show-error https://func-fdryvnetgw-bot-eastus.azurewebsites.net/api/health

  build-and-deploy-api:
    needs: terraform
    if: github.event_name == 'workflow_dispatch' && inputs.deploy_api
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-api.txt
      - name: Package API
        run: |
          mkdir api-package
          cp -r api config api-package/
          cp requirements-api.txt api-package/requirements.txt
      - uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.API_WEBAPP_NAME }}
          package: api-package

  build-and-deploy-ui:
    needs: terraform
    if: github.event_name == 'workflow_dispatch' && inputs.deploy_ui
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ui/package-lock.json
      - name: Install UI dependencies
        working-directory: ui
        run: npm ci
      - name: Build UI
        working-directory: ui
        run: npm run build
      - name: Deploy UI
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.UI_WEBAPP_NAME }}
          package: ui/www

  configure-apim:
    needs: [terraform, deploy-bot-function, build-and-deploy-api, build-and-deploy-ui]
    if: >-
      always() &&
      needs.terraform.result == 'success' &&
      contains(fromJson('[\"success\",\"skipped\"]'), needs.deploy-bot-function.result) &&
      contains(fromJson('["success","skipped"]'), needs.build-and-deploy-api.result) &&
      contains(fromJson('["success","skipped"]'), needs.build-and-deploy-ui.result)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deployment dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-deploy.txt
      - name: Configure APIM
        shell: pwsh
        run: ./scripts/configure-apim.ps1
      - name: Configure Foundry AI gateway via APIM
        shell: pwsh
        run: ./scripts/configure-foundry-ai-gateway.ps1

  provision-search-agents:
    needs: [terraform, deploy-bot-function, build-and-deploy-api, build-and-deploy-ui]
    if: >-
      always() &&
      needs.terraform.result == 'success' &&
      contains(fromJson('[\"success\",\"skipped\"]'), needs.deploy-bot-function.result) &&
      contains(fromJson('["success","skipped"]'), needs.build-and-deploy-api.result) &&
      contains(fromJson('["success","skipped"]'), needs.build-and-deploy-ui.result)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deployment dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-deploy.txt
      - name: Provision private Search and agents from source repo
        shell: pwsh
        run: ./scripts/provision-source-use-cases.ps1

  package-teams-agents:
    needs: [terraform, deploy-bot-function, build-and-deploy-api, build-and-deploy-ui]
    if: >-
      always() &&
      needs.terraform.result == 'success' &&
      contains(fromJson('[\"success\",\"skipped\"]'), needs.deploy-bot-function.result) &&
      contains(fromJson('["success","skipped"]'), needs.build-and-deploy-api.result) &&
      contains(fromJson('["success","skipped"]'), needs.build-and-deploy-ui.result)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Package Teams agents
        shell: pwsh
        run: ./scripts/package-teams-agents.ps1

  test:
    needs: [configure-apim, provision-search-agents, package-teams-agents]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deployment dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-deploy.txt
      - name: Test sample prompts
        shell: pwsh
        env:
          APP_API_BASE_URL: ${{ secrets.APP_API_BASE_URL }}
        run: ./scripts/test-sample-prompts.ps1
